# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Local Python setup

Use this Python interpreter for all Python commands:

`C:\ProgramData\anaconda3\python.exe`

Do not use `python`, `python3`, or `C:\Users\chenyon\anaconda3\python.exe`.

When running scripts from PowerShell, prefer:

`& "C:\ProgramData\anaconda3\python.exe" script_name.py`

## Project Overview

This is a research paper project folder (2026). No content has been added yet — populate this section with the paper's topic, research question, and target venue (journal/conference) as work begins.

## Expected Workflow

This directory will likely contain:
- Jupyter notebooks (`.ipynb`) for data analysis and figure generation
- Data files (`.csv`, `.xlsx`, etc.)
- Draft documents and notes

## Environment

- Python environment managed via Anaconda/conda (see `.condarc` in home directory)
- Jupyter notebooks are the primary analysis medium in this workspace
